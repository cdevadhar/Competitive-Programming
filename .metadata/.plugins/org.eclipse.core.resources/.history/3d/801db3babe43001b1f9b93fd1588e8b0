import java.io.*;
import java.util.*;
public class ABCs {
	
	int A;
	int B;
	int C;
	
//	public void findABCs(ArrayList<Integer> numbers) {
//		int ABC = Collections.max(numbers);
//		int minimum = Collections.min(numbers);
//		ArrayList<Integer> notABCs = new ArrayList<Integer>();
//		notABCs.add(ABC);
//		ArrayList<Integer> abcs = new ArrayList<Integer>();
////		for (int i=0; i<numbers.size(); i++) {
////			if (numbers.get(i)==minimum) {
////				System.out.println(numbers.get(i));
////				abcs.add(numbers.get(i));
////			}
////			
////		}
//		for (int i=0; i<abcs.size(); i++) {
//			notABCs.add(ABC-abcs.get(i));
//		}
//		for (int i=0; i<numbers.size(); i++) {
//			for (int j=0; j<numbers.size(); j++) {
//				for (int k=0; k<numbers.size(); k++) {
//					if (numbers.get(j) + numbers.get(k)==numbers.get(i)) {
//						notABCs.add(numbers.get(i));
//					}
//				}
//			}
//		}
//		for (int i=0; i<numbers.size(); i++) {
//			if (notABCs.contains(numbers.get(i))==false) {
//				abcs.add(numbers.get(i));
//			}
//		}
//		for (int i=0; i<abcs.size()-1; i++) {
//			
//			System.out.print(abcs.get(i) + " ");
//		}
//		System.out.print(abcs.get(abcs.size()-1));
//		
//		
//	}
	
	public void couldItBeABC(ArrayList<Integer> ABCs, int a) {
		if (a==Collections.min(ABCs)) {
			
		}
		for (int i=0; i<ABCs.size(); i++) {
			
		}
	}

	public static void main (String[] args) {
		Scanner scan = new Scanner(System.in);
		ArrayList<Integer> numbers = new ArrayList<Integer>();
		for (int i=0; i<7; i++) {
			int a = scan.nextInt();
			
			numbers.add(a);
		}
		
		
	}
}
