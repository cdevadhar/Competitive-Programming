import java.io.*;
import java.util.*;
public class MooyoMooyo {
	static int n, k;
	static int[][] grid;
	static Scanner in;
	public static void main(String[] args) {
		in = new Scanner(System.in);
		n = in.nextInt();
		k = in.nextInt();
		grid = new int[n][10];
		for (int i=0; i<n; i++) {
			for (int j=0; j<10; j++) {
				grid[i][j] = in.nextInt();
			}
		}
	}
	
	public static void floodfill(boolean[][] visited, int x, int y) {
		if (x>=n||x<0||y>=10||y<0) return;
		if (visited[x][y]) return;
		visited[x][y] = true;
		System.out.println(grid[x][y]);
		if (y+1<10) {
			if (grid[x][y]==grid[x][y+1]) {
				floodfill(visited, x, y+1);
			}
		}
		
		if (y-1>=0) {
			if (grid[x][y]==grid[x][y-1]) {
				floodfill(visited, x, y-1);
			}
		}
		
		if (x+1<10) {
			if (grid[x][y]==grid[x+1][y]) {
				floodfill(visited, x+1, y);
			}
		}
		
		if (x-1>=0) {
			if (grid[x][y]==grid[x-1][y]) {
				floodfill(visited, x-1, y);
			}
		}
		
	}
	
}
